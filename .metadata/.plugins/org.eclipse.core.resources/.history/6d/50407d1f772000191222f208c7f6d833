from BlogSys import urls
from django.db import connection
from Users.models import User

User.objects.filter(userID = 1).update(email_Address = '')
user_list = User.objects.all()

print(user_list)